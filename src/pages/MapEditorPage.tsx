import MapEditor from '../components/MapEditor.tsx';

/**
 * Standalone Map Editor Page
 * 
 * This page serves as the entry point for the map editor.
 * It can be easily extracted to a separate repository.
 * 
 * Route: /map-editor
 */
export default function MapEditorPage() {
  return <MapEditor />;
}
